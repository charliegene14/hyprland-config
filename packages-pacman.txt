# Paquets ajoutés par-dessus l'install ML4W stable de base.
# Tout ce qui suit a été ajouté APRÈS ML4W (vérifié par date d'install pacman).
# Le reste (rofi, swaync, wireplumber, nwg-dock-hyprland, jq, quickshell, networkmanager,
# pavucontrol, playerctl, libnotify, polkit, power-profiles-daemon, brightnessctl,
# blueman, network-manager-applet, cliphist, wl-clipboard, polices nerd, phosphor,
# material-symbols, font-awesome, etc.) est fourni par ML4W.

# Police Inter utilisée par le thème waybar premium
inter-font

# bluetoothctl (bluetooth-menu.sh) — pas inclus par ML4W stable
bluez-utils

# Optionnel : détection WireGuard par vpn.sh (`command -v wg`)
wireguard-tools
